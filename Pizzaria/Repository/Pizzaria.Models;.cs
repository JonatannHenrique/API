using Microsoft.EntityFrameworkCore;
using Pizzaria.Models;
namespace Pizzaria.Repository
{
    //internal class CadastroRepository : BaseRepositorio
    //{
    //  public CadastroRepository(AppDbContext repository) : base(repository)
    //    {
    //   }
    //    public async Task<Cadastro> GetLoginByasync(ulong id)
    //    {
    //        var logins = await _repository.Clientes
    //         .AsNoTracking()
    //         .FirstOrDefaultAsync(c => c.Id == id);
    //        return logins;
    //    }
    //}
}

